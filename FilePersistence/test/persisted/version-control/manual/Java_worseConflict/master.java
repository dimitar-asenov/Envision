class Foo {
	void foo() {
		a[longer].list();
		Go down;
		double trouble;
		fancy(fruit);
		a b;
		a c;
		stuff();
		bar();
		this.is(a[method]);
		this.is(newish);
		we.need();
		print();
		return val + b * c;
	}
}